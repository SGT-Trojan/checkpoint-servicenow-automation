---
offline: true
skip_list:
  # Imported workflow fragments predate the lint gate and remain syntax checked.
  - name[play]
  # Long Jinja assertions and command arguments are clearer as single YAML scalars.
  - yaml[line-length]
