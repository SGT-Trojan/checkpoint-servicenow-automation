---
offline: true
skip_list:
  # Playbook filenames already identify each workflow phase; syntax checks cover every file.
  - name[play]
  # Long Jinja assertions and command arguments are clearer as single YAML scalars.
  - yaml[line-length]
