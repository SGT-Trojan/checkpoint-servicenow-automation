---
- import_playbook: ../../ansible/playbooks/01_validate_activity_plan.yml
  vars:
    activity_plan_file: "{{ activity_plan_source }}"
    activity_plan: "{{ lookup('file', activity_plan_source) | from_json }}"
